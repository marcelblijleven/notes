# Beautify copied JSON
Copy the JSON that needs to be beautified / prettified and type the following into the command line
```
pbpaste | python -mjson.tool | pbcopy
```

Now you can paste the beautified json

